import { describe, it, expect } from "@jest/globals";


describe("#Add Program Form", () => {
  describe("#List", () => {
    it("Empty list of programs", () => {
      expect(true).toBe(true);
    })
  })
})